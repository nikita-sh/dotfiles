# nikita's dotfiles

my home manager and nixOS configs. not really "dotfiles" I guess.

## structure
- `systems`: system-level configurations for hosts
- `shared`: shared modules exported for use between systems
- `homes`: home-manager configurations
